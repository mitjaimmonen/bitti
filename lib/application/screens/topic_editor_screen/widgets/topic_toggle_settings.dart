import 'package:flutter/material.dart';

class TopicToggleSettings extends StatelessWidget {
  const TopicToggleSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
